# 工作流 02：内容排期与多平台分发自动化

> 难度：★★★☆☆ | 搭建时间：2-3小时 | 首选平台：n8n / Make.com

---

## 一、场景描述

### 谁需要这个工作流

**典型用户画像**：
- 自媒体创作者，同时运营抖音、视频号、小红书、公众号、B站
- 企业市场部，需要多渠道同步发布品牌内容
- MCN 机构，管理多个账号的内容排期
- 知识付费从业者，课程内容需要多平台分发引流

### 解决什么问题

**痛点**：
1. 同一篇内容要手动适配 5 个平台的格式（字数、图片尺寸、标签规则都不同）
2. 发布时间靠脑子记，经常漏发或撞期
3. 各平台数据散落，无法统一查看
4. 一个人管 5 个账号，光是复制粘贴就占了一半天时间

**解决方案**：
搭建内容自动化工作流——写一篇内容，AI 自动适配各平台格式，按排期定时发布，发布后自动汇总数据。

---

## 二、触发条件

| 触发方式 | 说明 | 适用场景 |
|---------|------|---------|
| 定时触发 | 按预设的排期时间自动发布 | 日常内容发布 |
| 手动触发 | 在表格中填入内容后点击执行 | 临时内容发布 |
| 新内容检测 | 飞书/Notion 文档状态变为「待发布」时触发 | 内容审核流程 |

---

## 三、完整处理步骤

```
内容输入（飞书表格 / Notion）
    ↓
[步骤1] 内容解析 — 读取原始内容、目标平台、发布时间
    ↓
[步骤2] AI 适配 — 按各平台规则改写内容
    ├─ 抖音版：短文案 + 话题标签 + 视频描述
    ├─ 视频号版：中等文案 + 位置信息
    ├─ 小红书版：标题优化 + 正文 + 标签 + 封面建议
    ├─ 公众号版：完整长文 + 封面图 + 摘要
    └─ B站版：动态文案 + 视频简介 + 标签
    ↓
[步骤3] 人工审核（可选）— 推送预览到飞书/钉钉，等待确认
    ↓
[步骤4] 定时发布 — 到达发布时间后自动推送各平台
    ↓
[步骤5] 数据回收 — 发布后定时拉取各平台阅读/点赞/评论数据
    ↓
[步骤6] 数据汇总 — 统一写入数据表格，生成效果对比
```

---

## 四、输出结果

### 直接输出

1. **5 个平台版本的内容文案**：自动生成并填入排期表
2. **定时发布任务**：按排期时间自动发布到各平台
3. **内容数据看板**：统一汇总各平台数据

### 数据汇总表

| 字段 | 说明 | 示例 |
|------|------|------|
| 内容标题 | 原始标题 | 「2026年AI工具选型指南」 |
| 发布日期 | 实际发布时间 | 2026-08-15 10:00 |
| 平台 | 发布平台 | 抖音/视频号/小红书/公众号/B站 |
| 播放/阅读量 | 曝光数据 | 12000 |
| 点赞数 | 互动数据 | 356 |
| 评论数 | 互动数据 | 28 |
| 转发/分享数 | 传播数据 | 45 |
| 涨粉数 | 转化数据 | 12 |
| 数据更新时间 | 最后拉取时间 | 2026-08-15 18:00 |

---

## 五、推荐工具组合

### 方案 A：n8n + 飞书方案（推荐）

| 组件 | 工具 | 用途 |
|------|------|------|
| 工作流引擎 | n8n | 编排整体流程 |
| 内容输入 | 飞书多维表格 | 内容排期管理 |
| AI 改写 | OpenAI GPT-4 API | 各平台文案适配 |
| 图片处理 | Cloudinary / 腾讯云 COS | 图片裁剪适配 |
| 平台发布 | 各平台开放 API | 自动发布 |
| 消息通知 | 飞书机器人 / 企业微信 | 审核通知 |
| 数据存储 | 飞书多维表格 | 数据汇总 |

### 方案 B：Make.com 方案（适合可视化操作）

| 组件 | 工具 | 用途 |
|------|------|------|
| 工作流引擎 | Make.com | 可视化拖拽编排 |
| 内容输入 | Google Sheets | 内容排期管理 |
| AI 改写 | OpenAI GPT-4 API | 各平台文案适配 |
| 平台发布 | Make.com 内置模块 | 支持 1000+ 应用集成 |
| 消息通知 | Slack / Telegram | 审核通知 |

### 各平台 API 说明

| 平台 | 发布 API | 注意事项 |
|------|---------|---------|
| 抖音 | 抖音开放平台 API | 需企业认证，支持视频发布 |
| 视频号 | 微信视频号 API | 需绑定公众号，能力有限制 |
| 小红书 | 小红书开放平台 API | 需企业号认证 |
| 公众号 | 微信公众平台 API | 支持图文素材管理和发布 |
| B站 | 哔哩哔哩开放平台 API | 支持动态和视频发布 |

> **重要提示**：各平台 API 能力和审核要求不同，部分平台暂不支持完全自动发布。对于不支持自动发布的平台，工作流会生成内容并推送通知提醒你手动发布。

---

## 六、详细配置步骤（以 n8n + 飞书为例）

### 准备工作

1. 注册 n8n 账号：访问 n8n.io，注册云端版（或自部署）
2. 注册飞书账号：访问 feishu.cn，创建团队
3. 获取 OpenAI API Key：访问 platform.openai.com，创建 API Key
4. 注册各平台开放平台账号并创建应用

### 第 1 步：创建飞书多维表格（内容排期表）

1. 在飞书中创建多维表格，命名为「内容排期与分发」
2. 创建两个数据表：

**表1：内容排期表**

| 字段名 | 字段类型 | 说明 |
|--------|---------|------|
| 标题 | 文本 | 内容标题 |
| 原始内容 | 多行文本 | 原始长文内容 |
| 内容类型 | 单选 | 图文/视频/纯文字 |
| 目标平台 | 多选 | 抖音/视频号/小红书/公众号/B站 |
| 发布时间 | 日期 | 预定发布时间 |
| 状态 | 单选 | 草稿/待审核/已适配/已发布/已失败 |
| 封面图片URL | 链接 | 封面图地址 |
| 备注 | 文本 | 特殊要求 |

**表2：发布数据表**

| 字段名 | 字段类型 | 说明 |
|--------|---------|------|
| 内容标题 | 文本 | 关联内容 |
| 平台 | 单选 | 发布平台 |
| 发布链接 | 链接 | 发布后的内容链接 |
| 发布时间 | 日期 | 实际发布时间 |
| 阅读量 | 数字 | 曝光/阅读数据 |
| 点赞数 | 数字 | 互动数据 |
| 评论数 | 数字 | 互动数据 |
| 数据更新时间 | 日期 | 最后拉取时间 |

3. 在飞书开放平台创建应用，获取 App ID 和 App Secret
4. 给应用授予多维表格读写权限

### 第 2 步：在 n8n 中创建工作流

1. 登录 n8n，点击「Add workflow」
2. 命名为「内容排期与多平台分发」

### 第 3 步：配置触发器（定时检查排期表）

1. 添加「Schedule Trigger」节点
2. 配置：
   - 触发频率：Every 10 minutes（每 10 分钟检查一次）
3. 添加「Fly.io / 飞书」节点（需先安装飞书节点或使用 HTTP Request 节点）
4. 如果使用 HTTP Request 节点调用飞书 API：

```
节点名称：查询待处理内容
请求方式：POST
URL：https://open.feishu.cn/open-apis/bitable/v1/apps/{APP_TOKEN}/tables/{TABLE_ID}/records/search
请求头：
  Authorization: Bearer {{飞书tenant_access_token}}
  Content-Type: application/json
请求体：
{
  "filter": {
    "conjunction": "and",
    "conditions": [
      {
        "field_name": "状态",
        "operator": "is",
        "value": ["待审核"]
      }
    ]
  }
}
```

5. 获取 tenant_access_token 的方法（单独添加一个 HTTP Request 节点）：

```
节点名称：获取飞书Token
请求方式：POST
URL：https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal
请求头：
  Content-Type: application/json
请求体：
{
  "app_id": "你的App ID",
  "app_secret": "你的App Secret"
}
```

### 第 4 步：配置 AI 内容适配节点

1. 添加「OpenAI」节点
2. 配置：
   - Resource：Chat
   - Operation：Create
   - Model：gpt-4o（或 gpt-4o-mini 降低成本）
   - System Message（系统提示词）：

```
你是一个多平台内容运营专家。请将以下原始内容按照各平台的特点进行改写。

# 原始内容
{{原始内容字段}}

# 目标平台
{{目标平台字段}}

# 各平台改写规则

## 抖音
- 文案不超过 100 字，节奏快
- 前 3 个字必须吸引眼球
- 结尾加 CTA（引导关注/评论）
- 话题标签 3-5 个，格式 #标签#
- 风格：口语化、接地气

## 视频号
- 文案 200-300 字，有观点
- 开头点出核心价值
- 结尾引导转发或关注
- 风格：理性、有温度

## 小红书
- 标题：15-20字，带数字或情绪词
- 正文：300-500字，分段清晰
- 使用 emoji 适度点缀（每段1-2个）
- 标签 5-8 个，格式 #标签#
- 风格：种草感、实用、有共鸣

## 公众号
- 标题：20-30字，信息完整
- 摘要：50字以内
- 正文：保持原文长度，优化排版
- 风格：深度、专业

## B站
- 动态文案：100字以内，有梗
- 视频简介：200-300字，包含时间戳
- 标签 5-10 个
- 风格：有趣、有料

# 输出格式
请以 JSON 格式输出，每个平台一个字段：
{
  "douyin": {"text": "文案内容", "tags": ["标签1","标签2"]},
  "weixin": {"text": "文案内容"},
  "xiaohongshu": {"title": "标题", "text": "正文", "tags": ["标签1","标签2"]},
  "gongzhonghao": {"title": "标题", "summary": "摘要", "text": "正文"},
  "bilibili": {"text": "动态文案", "description": "视频简介", "tags": ["标签1","标签2"]}
}
```

   - User Message：`{{原始内容}}`
   - Response Format：JSON Object

### 第 5 步：配置内容回写节点

1. 添加「HTTP Request」节点，将 AI 生成的各平台文案写回飞书表格
2. 或添加「Code」节点，解析 OpenAI 返回的 JSON，分别存储到对应字段

```
节点名称：解析并存储适配内容
代码（JavaScript）：
const response = JSON.parse(items[0].json.message.content);
return [
  { json: { platform: "抖音", content: response.douyin.text, tags: response.douyin.tags } },
  { json: { platform: "视频号", content: response.weixin.text, tags: [] } },
  { json: { platform: "小红书", content: response.xiaohongshu.text, tags: response.xiaohongshu.tags } },
  { json: { platform: "公众号", content: response.gongzhonghao.text, tags: [] } },
  { json: { platform: "B站", content: response.bilibili.text, tags: response.bilibili.tags } }
];
```

### 第 6 步：配置审核通知节点

1. 添加「飞书机器人 / 企业微信」节点
2. 推送审核通知到指定群：

```
节点名称：发送审核通知
请求方式：POST
URL：企业微信群机器人 Webhook
请求体：
{
  "msgtype": "interactive",
  "card": {
    "header": { "title": { "tag": "plain_text", "content": "📝 内容待审核" } },
    "elements": [
      { "tag": "div", "text": { "tag": "lark_md", "content": "**标题：** {{标题}}\n**发布时间：** {{发布时间}}" } },
      { "tag": "div", "text": { "tag": "lark_md", "content": "**抖音版：**\n{{抖音内容}}" } },
      { "tag": "div", "text": { "tag": "lark_md", "content": "**小红书版：**\n{{小红书内容}}" } },
      { "tag": "action", "actions": [
        { "tag": "button", "text": { "tag": "plain_text", "content": "通过并发布" }, "type": "primary", "value": { "action": "approve", "id": "{{记录ID}}" } },
        { "tag": "button", "text": { "tag": "plain_text", "content": "需要修改" }, "type": "danger", "value": { "action": "reject", "id": "{{记录ID}}" } }
      ] }
    ]
  }
}
```

### 第 7 步：配置定时发布节点

1. 添加「Schedule Trigger」节点（第二个触发器，用于发布）
2. 配置为每 5 分钟检查一次「已适配」状态且到达发布时间的内容
3. 添加「Switch」节点，按平台分流：

```
节点名称：按平台分流
条件1：平台 == "公众号" → 公众号发布节点
条件2：平台 == "B站" → B站发布节点
条件3：平台 == "抖音" → 抖音发布节点（或通知手动发布）
条件4：平台 == "小红书" → 小红书发布节点（或通知手动发布）
条件5：平台 == "视频号" → 视频号发布节点（或通知手动发布）
```

4. 以公众号发布为例，配置 HTTP Request 节点：

```
节点名称：公众号发布
请求方式：POST
URL：https://api.weixin.qq.com/cgi-bin/draft/add?access_token={{公众号access_token}}
请求体：
{
  "articles": [
    {
      "title": "{{标题}}",
      "author": "作者名",
      "digest": "{{摘要}}",
      "content": "{{正文HTML}}",
      "thumb_media_id": "{{封面图media_id}}",
      "need_open_comment": 1,
      "only_fans_can_comment": 0
    }
  ]
}
```

> 注意：公众号 API 先创建草稿，再调用 `freepublish/submit` 发布。部分平台不支持 API 直接发布，改为推送通知提醒手动操作。

### 第 8 步：配置数据回收节点

1. 添加「Schedule Trigger」节点，每天 10:00 和 18:00 各执行一次
2. 添加「HTTP Request」节点，调用各平台数据 API 拉取已发布内容的阅读/互动数据
3. 以公众号为例：

```
节点名称：拉取公众号数据
请求方式：GET
URL：https://api.weixin.qq.com/datacube/getarticlesummary?access_token={{token}}
请求体：
{
  "begin_date": "2026-08-15",
  "end_date": "2026-08-15"
}
```

4. 添加 HTTP Request 节点，将数据写回飞书的「发布数据表」

### 第 9 步：测试工作流

1. 在飞书表格中添加一条测试内容：
   - 标题：测试内容
   - 原始内容：随便写一段 200 字的内容
   - 目标平台：勾选抖音、小红书、公众号
   - 发布时间：设为 10 分钟后
   - 状态：改为「待审核」
2. 等待 n8n 触发，检查：
   - AI 是否生成了各平台版本
   - 审核通知是否推送到企业微信群
   - 审核通过后是否在指定时间发布
   - 数据是否回收到飞书表格

---

## 七、预估效果与 ROI

### 效果预估

| 指标 | 手动操作 | 自动化后 | 改善幅度 |
|------|---------|---------|---------|
| 单篇内容适配时间 | 40-60 分钟 | 2-3 分钟 | 提升 95% |
| 发布准时率 | 70% | 100% | 提升 30% |
| 数据统计耗时 | 30 分钟/天 | 0（自动） | 完全解放 |
| 漏发率 | 10-15% | 0% | 降为零 |

### 成本核算

| 项目 | 月成本 |
|------|--------|
| n8n 云端版 | 免费（1000次执行内） |
| OpenAI API（约 100 次调用） | $5-10 |
| 飞书 | 免费 |
| 各平台 API | 免费 |
| **合计** | **$5-10/月（¥35-70）** |

### ROI 计算

假设你每周发布 5 篇内容，每篇节省 50 分钟适配时间：
- 每周节省：5 × 50 分钟 = 250 分钟 ≈ 4 小时
- 每月节省：16 小时
- 按时薪 ¥50 计算，每月节省 ¥800
- **投入 ¥35-70/月，节省 ¥800/月，ROI ≈ 1000%+**

---

## 八、常见问题与解决方案

### Q1：部分平台 API 不支持自动发布怎么办

**解决**：
1. 对于不支持自动发布的平台（如抖音、小红书限制较严），工作流改为：
   - 生成该平台的内容版本
   - 推送通知到企业微信/飞书，附带文案内容
   - 提醒你手动复制粘贴发布
2. 使用浏览器自动化工具（如 Playwright）作为替代方案，但需注意平台合规性

### Q2：AI 生成的内容不符合平台调性

**解决**：
1. 在提示词中增加你过往的优秀内容作为示例（few-shot learning）
2. 增加平台特定的写作规则，如：
   - 小红书：必须有「姐妹们」「绝绝子」等社区用语（根据你的调性调整）
   - 抖音：前 3 秒必须有冲突或悬念
3. 在审核环节把关，不满意可以打回重新生成

### Q3：n8n 工作流执行失败

**解决**：
1. 在 n8n 工作流设置中开启「错误触发器（Error Trigger）」
2. 配置错误通知节点，失败时推送消息到飞书/企业微信
3. 常见失败原因：
   - API Token 过期 → 重新获取
   - 请求频率超限 → 增加 Delay 节点
   - JSON 解析失败 → 在 Code 节点增加 try-catch

### Q4：飞书 API 调用报错 99991663

**原因**：Token 未正确传递或权限不足

**解决**：
1. 确认飞书应用已授予 `bitable:app` 权限
2. 在飞书应用后台「权限管理」中检查多维表格相关权限
3. 确认 tenant_access_token 获取逻辑正确（有效期 2 小时，需要定期刷新）

### Q5：多平台发布时间需要错开吗

**建议**：
1. 同一内容在不同平台建议错开 30-60 分钟发布，避免平台判定为重复内容降权
2. 在 n8n 工作流中增加 Delay 节点，按平台顺序依次发布
3. 或在排期表中为不同平台设置不同的发布时间

### Q6：图片尺寸各平台要求不同怎么处理

**解决**：
1. 在 n8n 中添加 Cloudinary 或图片处理 API 节点
2. 按各平台要求自动裁剪：

| 平台 | 推荐图片尺寸 |
|------|-------------|
| 抖音封面 | 1080×1920（9:16） |
| 视频号封面 | 1080×1080（1:1） |
| 小红书封面 | 1080×1440（3:4） |
| 公众号封面 | 900×383（2.35:1） |
| B站封面 | 1146×717（16:10） |

3. 裁剪后上传到各平台的图片素材库，获取 media_id 用于发布

---

## 九、进阶优化方向

1. **热点追踪**：接入热搜 API，自动生成热点相关的内容选题
2. **A/B 测试**：同一内容生成 2 个标题版本，分别发布，对比效果
3. **智能排期**：根据各平台历史数据，自动推荐最佳发布时间
4. **跨平台引流**：在一个平台发布后，自动在其他平台发布引流内容
5. **评论聚合**：拉取各平台评论统一回复管理

---

*本指南基于 n8n 云端版和飞书开放平台 2026年8月版本编写。*
